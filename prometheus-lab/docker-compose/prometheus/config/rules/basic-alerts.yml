groups:
  - name: basic.rules
    rules:
      - alert: InstanceDown
        expr: up == 0
        for: 1m
        labels:
          severity: critical
        annotations:
          summary: "Instance down!"
          description: "{{ $labels.instance }} pole 1 minut jõudnud vastata!"
